/**
 * Deleted Post Notification System
 * (Modified - notifications disabled since they are now shown in the navbar bell icon)
 */

document.addEventListener('DOMContentLoaded', function() {
    // Notifications disabled - we now use the bell icon in the navbar
    // No periodic checks needed
});

/**
 * Check for deleted post notifications from the server
 * (Function disabled - we now use the bell icon in the navbar)
 */
function checkForDeletedPostNotifications() {
    // Function disabled - notifications now appear in the navbar bell icon
    return;
}

/**
 * Show a notification about a deleted post
 * (Function disabled - we now use the bell icon in the navbar)
 */
function showDeletedPostNotification(notification) {
    // Function disabled - notifications now appear in the navbar bell icon
    return;
} 